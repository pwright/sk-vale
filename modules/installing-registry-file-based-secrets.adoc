[id="installing-registry-file-based-secrets_{context}"]
:_mod-docs-content-type: CONCEPT
= File-based secrets for database credentials

[role="_abstract"]
{registry} supports file-based secrets, a generic `_FILE` suffix pattern that lets you provide credentials, such as database passwords, from files instead of environment variables. This approach is more secure than passing credentials directly as environment variables.

Instead of passing credentials and secrets as environment variables (which can be exposed in process lists and container
inspection), you can use file-based secrets. {registry} supports a generic `_FILE` suffix pattern that works with
any configuration property.

Quarkus automatically converts environment variables to configuration properties. For example,
`APICURIO_DATASOURCE_PASSWORD_FILE` becomes the property `apicurio.datasource.password.file`. The
FileBasedSecretsInterceptor reads the file path from these properties and loads the actual values from the files.

To use file-based secrets for any configuration property, take any environment variable name (for example, `APICURIO_DATASOURCE_PASSWORD`), add the `_FILE` suffix (for example, `APICURIO_DATASOURCE_PASSWORD_FILE`), and set the value to a file path containing the secret. {registry} reads the file contents and uses them as the actual property value.

This works for *any* configuration property, not only the credential variables listed in the following reference.

When a `_FILE` variant is set, the registry reads the value from the specified file instead of using the direct
environment variable. Configuration precedence: System properties (400) > File-based secrets (350) > Environment
variables (300) > `application.properties` (250).

**File format**

Secret files must contain only the credential value in plain text (UTF-8 encoding). Leading and trailing whitespace
is automatically trimmed. For example:

[source,text]
----
mypassword
----

**Security considerations**

* Ensure that secret files have restrictive permissions (for example, `chmod 600`)
* Never commit secret files to version control (add them to `.gitignore`)
* The registry logs a warning if secret files are world-readable
* If a `_FILE` variable is set but the file does not exist or is not readable, the registry fails to start with a
  clear error message

[role="_additional-resources"]
.Additional resources
* link:https://docs.docker.com/compose/how-tos/use-secrets/[Docker Compose secrets documentation]
* xref:installing-registry-file-based-secrets-reference_{context}[]


// Metadata created by nebel
// ParentAssemblies: assemblies/getting-started/as_installing-the-registry.adoc

// ref-installing-registry-file-based-secrets
