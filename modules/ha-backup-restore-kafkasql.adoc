[id="ha-backup-restore-kafkasql_{context}"]
:_mod-docs-content-type: PROCEDURE
= Backing up KafkaSQL storage

[role="_abstract"]
For KafkaSQL storage, back up the Kafka journal topic and snapshots topic.

.Procedure
* Use Kafka's MirrorMaker 2 or {kafka-streams} Mirror Maker for topic replication (recommended):
+
[source,yaml]
----
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaMirrorMaker2
metadata:
  name: registry-backup-mirror
spec:
  version: 3.5.0
  replicas: 1
  connectCluster: "backup-cluster"
  clusters:
    - alias: "source-cluster"
      bootstrapServers: registry-kafka-bootstrap:9092
    - alias: "backup-cluster"
      bootstrapServers: backup-kafka-bootstrap:9092
  mirrors:
    - sourceCluster: "source-cluster"
      targetCluster: "backup-cluster"
      topicsPattern: "kafkasql-.*"
      groupsPattern: ".*"
----
+
Alternatively, export and import topics using Kafka's console consumer or CLI tools like `kcat`/`kafkacat`. Since
the topics might contain binary data, which might be affected by text file encoding, make sure they are exported
correctly when using CLI tools.

[role="_additional-resources"]
.Additional resources
* xref:getting-started/guide-exporting-registry-kafka-topic-data.adoc[]

