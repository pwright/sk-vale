[id="kube-deleting-site-cli"]
= kube-deleting-site-cli


== Deleting a site using the CLI on Kubernetes

:_mod-docs-content-type: REFERENCE

[role="_abstract"]
Use the CLI to remove an existing Skupper site from Kubernetes.

*Prerequisites*

* The `skupper` CLI is installed.

*Procedure*

. Change context to the namespace where the site was created, for example:
+
[,bash]
----
kubectl config set-context --current --namespace west
----

. Enter the following command to delete a site:
+
[,bash]
----
skupper site delete
----

